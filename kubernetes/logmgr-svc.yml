apiVersion: v1
kind: Service
metadata:
  name: logmgr
  namespace: logging
  labels:
    app: logmgr
spec:
  ports:
    - port: 50001
      targetPort: 50001
      protocol: TCP
  selector:
    app: logmgr
