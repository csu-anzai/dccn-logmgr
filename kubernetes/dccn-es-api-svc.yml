apiVersion: v1
kind: Service
metadata:
  name: dccn-es-api
  namespace: logging
  labels:
    app: dccn-es-api
spec:
  ports:
    - port: 50001
      targetPort: 50001
      protocol: TCP
  selector:
    app: dccn-es-api
